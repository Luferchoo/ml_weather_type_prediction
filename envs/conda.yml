name: weather-mlops
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - pip
  - pip:
      - azure-ai-ml
      - azure-identity
      - pandas
      - numpy
      - scikit-learn==1.6.1    # Fijamos versión compatible
      - matplotlib
      - seaborn
      - azureml-mlflow
      - azureml-core
      - azureml-defaults
